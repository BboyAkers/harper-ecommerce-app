# Mirrors eslint.config.js's ignores, plus the things Prettier walks that ESLint
# never sees. Without this file, `npm run format` rewrites all of it.

# Build output and dependencies
node_modules/
dist/

# Generated by @harperfast/schema-codegen — regenerated on every boot, so
# formatting it is churn that comes straight back.
schemas/types.ts
schemas/globalTypes.d.ts

# Vendored from harperfast/skills and pinned by skills-lock.json. Reformatting
# these would put the working copy out of step with the lock for no benefit.
.agents/
skills-lock.json

# Lockfiles
package-lock.json
